const BINANCE_API = {
    API_DOMAIN: process.env.REACT_APP_BINANCE_API_DOMAIN,
    API_PREFIX: process.env.REACT_APP_BINANCE_API_PREFIX,
    ACCOUNT_INFORMATION: "/account"
};

const config = {
    BINANCE_API
};

export default config;
